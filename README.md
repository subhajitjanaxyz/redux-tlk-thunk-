# React + Vite

step 1 initialState
step 2 createAsyncThunk
step 3 createSlice
step 4 store 
step 5 Provider
step 6  

 useEffect(()=>{
    dispatch(alldataapi())
  },[])

  step 7 
    const {data,isloading,error}=useSelector((state)=>state.apidata)